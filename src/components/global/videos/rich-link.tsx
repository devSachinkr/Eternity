import React from 'react'

interface Props {
    id: string;
    description: string;
    source: string;
    title: string;
}

const RichLink = ({ id, description, source, title }: Props) => {
    return (
        <div>
        
        </div>
    )
}

export default RichLink
